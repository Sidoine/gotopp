AUX_SOURCE_DIRECTORY(commun COMMUN)
AUX_SOURCE_DIRECTORY(compilateur COMPILATEUR)
AUX_SOURCE_DIRECTORY(machine MACHINE)
AUX_SOURCE_DIRECTORY(type TYPE)
ADD_LIBRARY(gpp ${COMPILATEUR} ${COMMUN} ${MACHINE} ${TYPE})

