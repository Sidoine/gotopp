AUX_SOURCE_DIRECTORY(princ PRINC)
AUX_SOURCE_DIRECTORY(interne INTERNE)
LINK_DIRECTORIES(${GOTOPLUSPLUS_BINARY_DIR}/src})
ADD_EXECUTABLE(gotopp ${PRINC} ${INTERNE})

TARGET_LINK_LIBRARIES(gotopp ${GPP_LINK_LIBRARIES} ${CURL_LIBS} gpp boehmgc)
INSTALL_TARGETS(/bin gotopp gotopp)
